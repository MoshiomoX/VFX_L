// ============================================================
// PlayerFactory.h
// ????? Entity ??????1???????
//
// ??? CollisionTestScene::Init ? 30 ?????????
// ???????????????????????
//
// ?System ??????
//   PhysicsSystem / CollisionSystem ?????(???????????)?
//   ??????????????PlayerControlSystem ???????
//   ?????????????????UpdateGameplay ??????
//   (???????????????.)????????
//   System ?????? Scene ?????????
// ============================================================
#pragma once
#include "ECS/Entity.h"
#include <SimpleMath.h>

struct ID3D11Device;
class Registry;

namespace PlayerFactory
{
    using DirectX::SimpleMath::Vector3;
    using DirectX::SimpleMath::Vector4;

    // ============================================================
    // ???????
    // ????????????????? PlayerStatsComponent ??
    //   ImGui ????? Component ??????????????
    // ============================================================
    struct Config
    {
        Vector3 spawnPos = { 0.0f, 5.0f, 0.0f };

        float radius = 0.4f;
        float height = 1.0f;
        Vector4 color = { 0.3f, 0.6f, 1.0f, 1.0f };

        float moveSpeed = 5.0f;
        float jumpPower = 8.0f;

        float maxHealth = 100.0f;
        float maxMana = 50.0f;

        // ?????????????
        // ?VFX ??????????????????? false
        bool withWand = true;
        bool withBackpack = true;
    };

    // ============================================================
    // ??????1????
    // ????:Transform / Collider(Capsule) / Rigidbody / Model
    //          / PlayerTag / PlayerStats / PlayerState / Health
    //          / Wand / Backpack(Config ??)
    //
    // ?Wand ??????spells / areas ?????????????????
    //   ??????????????????????????????????
    // ============================================================
    Entity Create(Registry& reg, ID3D11Device* device, const Config& cfg = {});

    // ============================================================
    // ????????????????????????
    // ??????? PlayerStatsComponent?Config ?????
    //   ImGui ? radius ??????????????????????
    // ============================================================
    void RebuildVisual(Registry& reg, Entity player, ID3D11Device* device,
        const Vector4& color);
}