// ============================================================
// WandComponent.h
// ?????????????????(spells)??????
// spells ??????????????1?? ImGui ????????
// ============================================================
#pragma once
#include <SimpleMath.h>
#include <vector>
#include "SpellID.h"
#include "Component/AreaStats.h"
// 1??????????(?????)
enum class CastMode
{
    Auto,        // ???????????(?????)
    Manual,      // ?????????????????
    DebugBurst,  // castInterval ??????????(?????????)
};
struct SpellStats
{
    ItemID id = ItemID::Fireball;

    // --- ??(????:??????????)---
    int   projectileCount = 1;      // ??????
    float spreadAngle = 0.0f;   // ???(?)?count>1 ??????

    // --- ????(????:???????????)---
    int   castCount = 1;            // 1??????????
    float castDelay = 0.12f;        // ?????(?)

    // --- ??? / ??? ---
    float castInterval = 0.5f;      // ????(?)????????
    float manaCost = 10.0f;     // 1??????(castCount ?????)

    // --- ???????? ---
    float damage = 10.0f;
    float speed = 20.0f;
    float radius = 0.25f;
    float lifetime = 3.0f;

    // --- ?????(WeaponSystem ???)---
    float castTimer = 0.0f;      // ?????????
    int   pendingCasts = 0;         // ???????????
    float delayTimer = 0.0f;      // ?????????
};

struct WandComponent
{
    // --- ??(????????? AOE ?????)---
    float manaMax = 100.0f;
    float manaCurrent = 100.0f;
    float manaRegen = 25.0f;

    float range = 15.0f;
    DirectX::SimpleMath::Vector3 muzzleOffset = { 0.0f, 0.5f, 0.0f };

    CastMode castMode = CastMode::Auto;
    bool     castRequested = false;
    float castAnimTimer = 0.0f;
    float castAnimDuration = 0.3f;
    // --- ??????(?????????????????????)---
    std::vector<SpellStats> spells;   // ????
    std::vector<AreaStats>  areas;    // AOE ?
};