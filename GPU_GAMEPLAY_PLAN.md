# GPU Gameplay 管线移行 计划书

> 作成日: 2026-09-03
> 対象: VFX_L / 雑魚・投射物・経験値オーブの GPU 化

---

## 1. 背景と目標

### 現状の計測値

| 項目 | 値 |
|---|---|
| 敵 114 体時 | 165 FPS → 120 FPS |
| フレーム時間 | 6.06ms → 8.33ms |
| 1体あたりの増分 | **約 0.02ms** |
| 内訳 | 描画・線框を切っても変化なし = **全部 CPU の gameplay** |

### 線形外挿

```
 100 体 →  8.3ms → 120 FPS
 500 体 → 16.0ms →  62 FPS
1000 体 → 26.0ms →  38 FPS
3000 体 → 66.0ms →  15 FPS
```

### 目標

**3000 体まで 60 FPS 以上**を維持する。
そのために雑魚・投射物・経験値オーブの全経路（AI / 移動 / 衝突 / 描画）を
GPU 上で閉じ、CPU は入力と提示だけを行う。

---

## 2. 設計方針（決定済み・再議論しない）

### 2-1. CPU / GPU の分割線

| 側 | 対象 | 理由 |
|---|---|---|
| **GPU** | 雑魚 (MobTag) / 投射物 / 経験値オーブ | 同構・大量・論理が単純 |
| **CPU** | 玩家 / 精英・ボス (EliteTag) / UI / バックパック | 異構・少数・手感と調試が命 |

`MobTag` / `EliteTag` の分割が、そのまま GPU / CPU の分割線になる。
これは偶然ではなく「同構かつ大量」と「異構かつ少数」が同じ区分だから。

**玩家を GPU に載せない理由**
- 入力遅延: CPU→GPU→画面 で最低 1〜2 フレーム増える。手感が死ぬ
- 分岐発散: 状態機（3層）・杖の複数出力源・集約は warp が割れる
- 調試喪失: ImGui の状態機パネル・集約ログが読めなくなる
- そもそも 1 体のために dispatch するのは CPU より数十倍遅い

### 2-2. 空間分割は作らない

全対全の暴力計算で足りる。

| 体数 | 総当たり回数 | GPU 概算 |
|---|---|---|
| 1000 | 100万 | 約 0.05ms |
| 3000 | 900万 | 約 0.3ms |
| 10000 | 1億 | 約 3ms |

GPU 上のハッシュ構築（cell id → ソート → 境界検出）は工程中で最難だが、
3000 体規模なら払う必要がない。**分離の 6 近傍上限も撤廃できる**
（全部見られるので、密集時に押し開けない問題が同時に解決する）。

### 2-3. 固定ステップ物理

現状は dt が可変なので、フレームレートが gameplay パラメータになっている。
25fps で dt=40ms、敵は1フレームで 14cm 進む。60fps なら 5.8cm。
同じ障害物でもコマ落ち時のほうが貫通しやすく、押し出し深度も変わる。

→ 累加器方式の固定ステップに変更。1フレームで 2〜3 子ステップ回る。
CPU で 3 回衝突を回すのは贅沢だが、GPU なら dispatch 2 回追加なので実質無料。

**既に同じ手法を2箇所で使っている**: SpellbookUI の 1/120 積分、
GPUParticleSystem の Flush。今回は gameplay 層へ広げるだけ。

### 2-4. 回読は counter のみ

| 用途 | サイズ | 遅延1〜2フレームの影響 |
|---|---|---|
| 生存雑魚数（湧き制御） | 4 B | 無し（湧き間隔は1秒） |
| 撃破総数（UI） | 4 B | 無し |
| 玩家の累計被弾 | 4 B | 無し（無敵時間 0.6 秒） |

**合計 16 バイト。** データ量は無視できる。真のコストは**同期**。

- `Map(D3D11_MAP_READ)` は GPU 完了を待つ = 管線切断 = 数ms 損失
- 必ず **ring buffer（staging 3枚輪転）+ `D3D11_MAP_FLAG_DO_NOT_WAIT`**
- 書き終わっていなければ即失敗を返し、CPU はスキップして進む

**硬い制約: 阻塞式 Map READ は如何なる場合も禁止。**

### 2-5. その他の決定

- **経験値オーブも一緒に GPU へ。** CPU に残すと毎フレーム数十件の
  死亡イベントを回読して Entity を作ることになり、回読量が増える
- **SpawnDirector の押し出しは「転送回収」へ変更。**
  GPU 上で最遠を探す reduction + ソートは高い。
  距離超過の敵を削除せず、環帯上の新座標を書き直して HP 全快する。
  体感は削除方式と同一（逃亡プレイは依然として成立しない）
- **固定スロットを使い、consume buffer を避ける。**
  `CopyStructureCount` は命令キューをフラッシュするため約 0.076ms の
  固定コストがある（粒子で計測済み）。粒子では 0.6% で無視できたが、
  8.3ms → 1ms を狙う今回は 30% になる

---

## 3. 参照する既存実装

### GPUParticleSystem の管線（すでに完全な GPU 閉環）

```
[CPU 収集]  各 VFXEffect → SubmitEmitters() → m_PendingEmitters 累積
     ↓
[Flush 1回] Upload : Map(WRITE_DISCARD) emitter + colorKey    0.018ms
            Emit   : CopyStructureCount(deadCount ← deadList)
                     Dispatch EmitCS                           0.076ms
            Update : Clear(DrawArgs) + Dispatch UpdateCS       0.025ms
     ↓
[Render]    DrawInstancedIndirect(DrawArgs)
```

**そのまま流用するもの**
- deadList による空きスロット管理（弾も敵も「プール + 再利用」）
- AliveList + DrawIndirect（描画も GPU が数を決める）
- Submit → Flush の二段式（CPU が溜め、1回だけ dispatch）
- 「数の真実は GPU にしかない」という原則

**粒子に無く、今回新規に要るもの**
- 回読通道（ring buffer + DO_NOT_WAIT）
- 模型付き instancing（粒子は VS で quad 展開、敵は頂点バッファ + instance 変換）

---

## 4. Phase 一覧

### Phase 0 — 計測 · 0.5h

分段計時（`timeIt` ラムダで各 System を包む + ImGui で ms 表示）。

**産出**: 8.3ms の内訳表。
Phase 2 と 3 の順序を決める根拠であり、以降すべての効果測定の基準線。

---

### Phase 1 — 基盤 · 3〜4h

| 項目 | 内容 |
|---|---|
| `GPUWorld` クラス | 全 gameplay buffer の保持と dispatch 順序の管理 |
| 回読通道 | ring buffer（staging 3枚）+ DO_NOT_WAIT の汎用 counter 回読 |
| 玩家アップロード | 毎フレーム CB 1個（位置 / 半径 / HP）、単向 |
| 地形アップロード | GridWorld の `walkable[]` を起動時に1回、読み取り専用 SRV |

**検収**: テスト用の数値を1つ上げて読み戻せる。阻塞しない。

---

### Phase 2 — 投射物 · 6〜8h

先頭に置く理由:
1. 消費鎖が最短（飛行 → 命中 → 消滅）。CPU 側にほぼ読者が居ない
2. 数が最多（分裂 + 二重釈放で数百〜数千が常駐）
3. 既に半分 GPU 上（BillboardRenderer が毎フレーム位置を上げている）
   → 載せ替えると逆にアップロードが消える
4. **collider 数の大半を占める**。抜けば CPU 側の衝突負荷が即半減

| 子任務 | 工時 |
|---|---|
| projectiles buffer + 空きスロット管理 | 1.5h |
| 生成経路（WeaponSystem → GPU 書き込みキュー） | 1.5h |
| `ProjectileMoveCS`（固定ステップ積分 + 寿命） | 1h |
| 描画改造（VS が buffer を直読 + DrawIndirect） | 1.5h |
| 調試 | 2h |

**検収**: 弾が GPU 上で飛び、描画され、寿命で消える。まだ当たらない。

---

### Phase 3 — 雑魚の移動 · 6〜8h

| 子任務 | 工時 |
|---|---|
| enemies buffer + スロット管理 | 1h |
| `EnemyAICS`（seek + separation 全対全 + 格子回避） | 2.5h |
| 固定ステップ循環（累加器、1フレーム 2〜3 子ステップ） | 1h |
| instanced 描画 + DrawIndirect | 1.5h |
| 調試 | 2h |

**検収**: 敵が GPU 上で追跡し、壁を抜けず、一点に重ならない。
まだ倒せず、ダメージも受けない。
**ここで初めて性能改善が数値で見える。**

---

### Phase 4 — 戦闘の閉環 · 6〜8h

| 子任務 | 工時 |
|---|---|
| `HitCS`（弾 vs 敵、全対全。命中で HP 減 + 弾を消滅マーク） | 2h |
| `ContactCS`（敵 vs 玩家、ダメージを回読 buffer へ累加） | 1h |
| 死亡処理 + 経験値オーブの GPU 化 | 3h |
| 調試 | 2h |

**検収**: 敵を倒せる、オーブが落ちる、囲まれると死ぬ。
**ゲームループが GPU 上で閉じる。**

---

### Phase 5 — 湧きと後始末 · 4〜6h

| 子任務 | 工時 |
|---|---|
| SpawnDirector の GPU 移行（転送回収を含む） | 2h |
| ImGui パネル書き直し（一部だけ回読して検視） | 1.5h |
| CPU 側の残骸整理（`m_Enemies` / HitEvent 経路 / EliteTag の旧経路保持） | 2h |

---

## 5. 工時合計と現実的な日程

```
Phase 0    0.5h
Phase 1    3〜4h
Phase 2    6〜8h
Phase 3    6〜8h
Phase 4    6〜8h
Phase 5    4〜6h
─────────────────
合計      26〜35h
```

**2日で全部は非現実的**（13〜17h/日 が必要）。

現実的な2日目標: **Phase 0〜3（16〜20h）**
→ 敵と弾が GPU 上で動き、性能改善が測定可能。まだ相互にダメージは無い。
Phase 4〜5 は3日目。

---

## 6. リスク

1. **CS にブレークポイントが無い。** 誤りは「真っ黒」「全部飛んでいく」と
   いう形で出るので、buffer を回読して目視比較するしかない。
   調試時間を全体の 30% として計上済み
2. **Phase 5 の雑事を過小評価しやすい。**
   「あ、ここでも Entity を使っていた」が1件30分
3. **二つの世界の接ぎ目。** 精英が CPU、雑魚が GPU。
   現在は跨界の相互作用が無いので問題ないが、
   「ボスが小怪を召喚」「小怪がボスを押す」が出た時に再設計が要る

---

## 7. 将来の天井

GPU 化後の律速は gameplay ではなく**描画**になる。
1万体を instanced で描いても、GPU は1万個のカプセルを塗る必要がある。
その時に必要になるのは LOD と視錐台カリング。

参考: Unity の群衆プラグイン Enemy Masses の性能階梯では、
**アニメーション方式が体数の天井を決める**とされている。

| 方式 | 規模 |
|---|---|
| GPU 焼き込みアニメ | 5,000〜10,000+ |
| CPU ボーン追跡 | 1,000〜3,000 |
| 標準 Animator | 100〜500 |

計画表 第4歩（玩家細節・アニメーション）で敵にも骨格アニメを入れる場合、
**AI ロジックより先にアニメ方式が制約になる**。

---

## 8. 次の行動

**Phase 0 を実行 → 8.3ms の内訳を取得 → Phase 2 と 3 の順序を確定。**

- 分離（O(n²)）が支配的 → 雑魚を先に
- 衝突の収集・登記が支配的 → 投射物を先に
- 分散している → 計画通り投射物 → 雑魚
